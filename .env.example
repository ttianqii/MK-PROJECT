# MariaDB connection (matches the db service in compose.yaml).
DATABASE_URL=mysql://mk:mkpassword@127.0.0.1:3306/mkproject

# HMAC secret for signing the session cookie. Use a long random value in
# production, e.g. `openssl rand -hex 32`.
SECRET_KEY=dev-secret-change-me
